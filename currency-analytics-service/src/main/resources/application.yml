#spring.application.name=currency-analytics-service

spring:
  application:
    name: currency-analytics-service

  datasource:
    driver-class-name: org.postgresql.Driver
    username:
    password:
    url:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        show_sql: true

server:
  port: 8083